eg.) =>git log
	we can see thenn logs with commit id,date.

For Delete the branch from remote and locally

	Locally
	eg.)git branch -d <branch name>

	Remotly
	eg.) git push origin --delete <branch name>


Remove from tracked file [from stage]
	File
		git  rm  --cached <filename>
	Folder
		git  rm -r --cached <Folder name>
  
